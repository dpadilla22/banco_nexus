MONGO_URI=mongodb+srv://<usuario>:<password>@<cluster>.mongodb.net/nexus_banca
DB_NAME=nexus_banca
PORT=3000

# Optional. Leave empty/0 if the data load is still being defined.
EXPECTED_CLIENTES=0
EXPECTED_CUENTAS=0
EXPECTED_TRANSACCIONES=0

# JWT
JWT_SECRET=cambia_esto_por_una_clave_secreta_larga
JWT_EXPIRES=8h